# gres.conf - Slurm Generic Resource configuration

# Auto-detect NVIDIA GPUs using NVML
AutoDetect=nvml

# Manual configuration example (if AutoDetect doesn't work):
# NodeName=compute-[001-010] Name=gpu Type=tesla File=/dev/nvidia[0-3]
